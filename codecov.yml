coverage:
  status:
    project:
      default:
        informational: true
    patch:
      default:
        informational: true

comment:
  layout: 'reach,diff,flags,tree,components,tests,build'
  behavior: default
  require_changes: false
